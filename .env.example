# Google Gemini (existing)
GEMINI_API_KEY=your_gemini_api_key

# Canadian address postal codes — server-side only (do NOT expose in client builds)
# Get token from https://geocoder.ca/api
GEOCODER_CA_AUTH=your_geocoder_ca_auth_token

# Maps use Leaflet + OpenStreetMap (free, no API key required)
